services:
  expense-tracker:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
    networks:
      - default
      - observability

networks:
  observability:
    external: true
